import ScrollVideo from "@/components/ScrollVideo";

export default function HomePage() {
  console.log("[page] HomePage render — ScrollVideo only (end-of-demo section removed)");

  return (
    <main>
      <ScrollVideo />
    </main>
  );
}
